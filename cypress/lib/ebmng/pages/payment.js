const utils = require('../../utils');
// const checkoutSchema = require('../schemas/checkout');

const paymentPage = (function (test) {
  const _private = {
    elements: {
      buttons: {

      },
      containers: {

      },
      fields: {

      }
    }
  };

  const { buttons, fields, containers } = _private.elements;

  const $public = {

  };

  return $public;
});

module.exports = paymentPage;